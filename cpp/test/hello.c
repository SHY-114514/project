#include<stdio.h>

void QuickSort(int arr[], int left, int right){
    if(left > right)
        return ;
    int i = left,n = right,v = arr[left];
    while(left < right){

    }
}

int main(void){
    printf("Hello World!\n");
    return 0;
}
